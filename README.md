# 리커버핏 Coach (recovery-coach)

간단한 설문으로 현재 몸 상태를 확인하고, **운동 추천 · 회복 루틴 · 영양 가이드**를 안내하는 모바일 앱입니다.
학교 체육교사가 옆에서 코칭해 주는 느낌으로, 앱 화면만 보고도 안전하게 운동할 수 있도록 만드는 것이 목표입니다.

> **현재 상태: 1차 MVP 동작** — 설문 → 위험 신호 확인 → 운동·영양 추천 → 루틴 실행 → 기록까지
> 앱 안에서 흐름이 끊기지 않고 이어집니다.
> **운동·영양 콘텐츠는 아직 전문가 검수 전입니다. 실제 사용자에게 배포하기 전에 검수가 필요합니다.**

---

## ⚠️ 먼저 읽어 주세요

이 앱은 **의료기기가 아니며, 의학적 진단·치료·처방을 제공하지 않습니다.**

- 제공하는 것: 일반적인 **운동 추천**, **회복 루틴**, **영양 가이드**, **기록 도구**
- 제공하지 않는 것: 진단, 치료, 처방, 병명 판정, 응급 대응
- 위험 신호가 확인되면 **운동 루틴을 제공하지 않고 전문가 상담을 안내**합니다.
- 이미 진료를 받고 있다면 **의료진의 지시가 항상 우선**합니다.

자세한 기준은 [docs/SAFETY_POLICY.md](docs/SAFETY_POLICY.md) 를 참고하세요.

---

## 핵심 원칙

| # | 원칙 |
|---|---|
| 1 | 의학적 진단·치료·처방을 하지 않는다 |
| 2 | 화면에서는 "처방"이 아니라 "운동 추천 / 회복 루틴 / 영양 가이드"라고 쓴다 |
| 3 | 위험 신호가 있으면 루틴을 주지 않고 전문가 상담으로 안내한다 |
| 4 | 학생 사용자에게 무리한 다이어트·칼로리 제한을 유도하지 않는다 |
| 5 | 1차 버전은 서버 없이 **기기 로컬 저장**만 사용한다 |
| 6 | 추천은 AI 생성이 아니라 **안전한 룰 기반 엔진**으로 만든다 |
| 7 | UI는 깔끔하고 신뢰감 있게, 코칭하는 톤으로 만든다 |
| 8 | 한국어 UI 기본 |

---

## 주요 기능 (계획)

1. 온보딩
2. 안전 안내 및 동의
3. 초기 설문 (안전 스크리닝 → 부위·통증 → 활동·목표 → 영양)
4. 운동 추천 결과
5. 운동 루틴 실행 화면
6. 영양 추천 결과
7. 기록 화면
8. 설정 화면

### 운동 카테고리
체형교정 · 동적웜업 · 기능성 운동 · 어깨 회복 · 허리 회복 · 무릎 회복 · 발목 회복 · 목/등 상부 회복

### 회복운동 4단계 구조
| 단계 | 내용 |
|---|---|
| Phase 1 | 통증 완화 / 가동성 회복 |
| Phase 2 | 안정화 / 근육 활성화 |
| Phase 3 | 근력 회복 |
| Phase 4 | 기능 복귀 / 재발 예방 |

### 영양 트랙
다이어트·건강 체중관리 (성인 전용) · 벌크업/근육량 증가 · 일반 건강식 · 회복/면역/미네랄

> **학생 모드**에서는 칼로리 숫자 대신 **손바닥 / 주먹 / 엄지 기준**의 균형식 가이드를 제공합니다.

---

## 기술 스택

| 영역 | 선택 |
|---|---|
| 프레임워크 | Expo (React Native) |
| 언어 | TypeScript (strict) |
| 라우팅 | Expo Router |
| UI | React Native Paper + 커스텀 테마 |
| 로컬 저장 | AsyncStorage |
| 콘텐츠 | 로컬 TypeScript / JSON |
| 테스트 | Jest (룰 엔진 단위 테스트 중심) |

---

## 동작 영상 연결하기

앱은 영상을 **가지고 있지 않습니다.** 링크만 보관하고, 누르면 유튜브가 열립니다.
영상이 없는 운동에는 **"유튜브에서 찾아보기"** 버튼이 나옵니다 (지금도 동작합니다).

```bash
npm run videos:check   # 몇 개 연결됐는지 확인
```

| 문서 | 내용 |
|---|---|
| [docs/VIDEO_GUIDE.md](docs/VIDEO_GUIDE.md) | 저작권 규칙, 영상 고르는 기준 |
| [docs/VIDEO_CANDIDATES.md](docs/VIDEO_CANDIDATES.md) | 1순위 운동 후보 링크 (**검수 전**) |
| [docs/MEDIA_PRIORITY.md](docs/MEDIA_PRIORITY.md) | 86개 제작 우선순위 |

> **링크는 되고, 파일은 안 됩니다.** 영상을 내려받아 앱에 넣는 것은 저작권 침해입니다.
> 조회수가 높다고 안전한 영상은 아닙니다. 선정 기준은 VIDEO_GUIDE 를 따르세요.

## 동작 이미지 추가하기

앱은 지금 **글 설명 + 부위 표시 그림**으로 동작합니다. 사진이 없어도 정상 동작합니다.
사진을 넣으면 자동으로 그 자리에 나옵니다.

```bash
npm run images:list   # 필요한 이미지 목록을 docs/IMAGE_LIST.md 로 생성
npm run images:check  # 지금 몇 개가 준비됐는지 확인
```

1. `assets/exercises/` 에 파일을 넣습니다. 파일명은 운동 id 와 같게 (`knee-p1-01.png`)
2. `src/data/exerciseImages.ts` 에 한 줄 추가
3. 앱을 다시 실행

**반드시 지킬 것**
- 얼굴이 알아볼 수 있게 나온 사진은 쓰지 않습니다 (초상권·개인정보)
- 학생을 모델로 쓰지 않습니다. 꼭 필요하면 본인·보호자 동의를 먼저 받으세요
- 직접 제작했거나 상업적 이용이 가능한 이미지만 씁니다
- **자세가 틀린 이미지는 글 설명보다 위험합니다. 확신이 없으면 넣지 마세요**

## 문서

| 문서 | 내용 |
|---|---|
| [docs/PRD.md](docs/PRD.md) | 제품 목표, 페르소나, 사용자 흐름, 화면 구조, 설문 구조, MVP 범위 |
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | 폴더 구조, 데이터 타입, 룰 엔진 설계, 저장소 설계 |
| [docs/SAFETY_POLICY.md](docs/SAFETY_POLICY.md) | **안전 정책 — 다른 모든 요구사항보다 우선** |
| [docs/CONTENT_GUIDE.md](docs/CONTENT_GUIDE.md) | 운동·영양 콘텐츠 작성 규칙, Phase 정의, 검수 체크리스트 |
| [docs/ROADMAP.md](docs/ROADMAP.md) | 마일스톤 M0~M8, 단계별 체크리스트, 리스크 |
| [docs/IMAGE_LIST.md](docs/IMAGE_LIST.md) | 동작 이미지 86개 제작 목록 (자동 생성) |
| [docs/MEDIA_PRIORITY.md](docs/MEDIA_PRIORITY.md) | 이미지·영상 제작 우선순위 (자동 생성) |
| [docs/VIDEO_GUIDE.md](docs/VIDEO_GUIDE.md) | 영상 저작권 규칙과 선정 기준 |
| [docs/VIDEO_CANDIDATES.md](docs/VIDEO_CANDIDATES.md) | 영상 후보 링크 (검수 전) |

---

## 개인정보 / 건강정보 취급

- 설문 응답과 운동 기록은 **기기 안에만 저장**되며 외부로 전송되지 않습니다.
- 이름·연락처·학번·소속 학교는 **입력받지 않습니다.**
- 이 저장소에는 **실제 사용자 데이터를 올리지 않습니다.**
- `.env`, 서명 키, 인증서는 `.gitignore` 로 커밋을 차단합니다.

---

## 개발 현황

- [x] M0 기획 문서 작성 (PRD / ARCHITECTURE / SAFETY_POLICY / CONTENT_GUIDE / ROADMAP)
- [ ] M0 전문가 검수 (건강운동관리사 · 물리치료사 · 의사 · 영양 전문가)
- [x] M1 프로젝트 셋업 (Expo + TypeScript + Expo Router + 테마 + 저장소 + 테스트)
- [x] M2 안전 레이어 (동의 · 위험 신호 체크 · 상담 안내 · 전역 라우트 가드)
- [x] M3 설문 & 룰 기반 추천 엔진
- [x] M4 운동 콘텐츠 86개 · 프로그램 23개 · 루틴 실행 화면
- [x] M5 영양 4트랙 · 기록 · 내정보
- [x] 빠른 시작(설문 건너뛰기) · 웹 배포 지원
- [ ] M6 다듬기 (이미지·접근성·실기기)
- [ ] M7 **전문가 검수** 및 출시 준비

전체 계획은 [docs/ROADMAP.md](docs/ROADMAP.md) 참고.

---

## 실행 방법

### 휴대폰에서 보기
```bash
npm install        # 필요한 라이브러리 설치 (처음 한 번)
npx expo start     # 개발 서버 실행 → 폰의 Expo Go 앱으로 QR 스캔
```

### 웹 브라우저에서 보기
```bash
npm run web        # 개발 중 브라우저로 확인
```

### 웹으로 배포하기
```bash
npm run build:web  # dist/ 폴더에 정적 웹사이트 생성
npm run serve:web  # 만들어진 결과를 로컬에서 확인
```

`dist/` 폴더를 그대로 올리면 됩니다. GitHub Pages, Netlify, Vercel 등 정적 호스팅이면
어디든 동작합니다. 빌드할 때 아래 파일이 함께 만들어집니다.

| 파일 | 역할 |
|---|---|
| `404.html` | GitHub Pages 에서 주소를 직접 입력해도 앱이 열리게 함 |
| `_redirects` | Netlify 에서 같은 역할 |
| `.nojekyll` | `_expo` 폴더가 무시되지 않게 함 |

확인용 명령어

```bash
npm run typecheck    # 타입 오류 검사
npm run lint         # 코드 규칙 검사
npm test             # 테스트 실행
npm run videos:check # 영상 링크가 실제 운동과 맞는지 확인
npm run images:check # 이미지 등록표 확인
```

### 자동 검사 (GitHub Actions)

`.github/workflows/ci.yml` 이 **코드를 올릴 때마다 자동으로** 위 검사를 돌립니다.
직접 명령어를 치지 않아도 PR 화면 아래쪽에 결과가 뜹니다.

| 항목 | 무엇을 잡나 |
|---|---|
| 타입 검사 | 없는 값을 화면에서 꺼내 쓰는 실수 |
| 코드 규칙 | 쓰지 않는 변수, 잘못된 훅 사용 |
| 테스트 | **안전 규칙이 깨졌는지** (아래 참고) |
| 영상 링크 확인 | 없는 운동에 영상이 걸려 있는지, 링크 형식이 맞는지 |
| 이미지 등록표 확인 | 등록은 했는데 파일이 없는 운동이 있는지 |
| 웹 빌드 | 실제로 빌드가 되는지 |

테스트가 지키고 있는 안전 규칙 (깨지면 빨간불이 뜹니다)

- 위험 신호가 있으면 어떤 경로로도 운동 루틴이 나오지 않는다
- 설문 건너뛰기 목록에 위험 신호 문항이 절대 들어가지 않는다
- 학생 모드에서 숫자 목표(칼로리·탄단지 비율)가 나오지 않는다
- 손상 교육 자료에 재활 기간이 '몇 주' 로 적히지 않는다
- 영상 관리 화면에 앱 안에서 영상을 재생하는 도구가 들어가지 않는다
- 2026-09-02 점검에서 제외한 영상 6개가 다시 등록되지 않는다

**빨간불이 떴을 때** — PR 화면에서 실패한 항목 이름을 누르면 어느 파일 몇 번째 줄에서
무엇이 걸렸는지 그대로 나옵니다. 저장소 Actions 탭에서 직접 다시 돌릴 수도 있습니다.

### 웹 지원 범위

- 설문 → 추천 → 루틴 실행 → 기록까지 **모든 기능이 브라우저에서 동작**합니다.
- 넓은 화면에서는 본문 폭을 520px로 제한하고 가운데 정렬합니다.
- 기록은 브라우저의 로컬 저장소에 남습니다. **브라우저 기록을 지우면 함께 사라집니다.**
- 뒤로가기 차단(상담 안내 화면)은 웹에서도 동작합니다.

### 폴더 구조

```
app/
  onboarding/     온보딩, 안전 안내 및 동의
  assessment/     설문, 결과, 상담 안내(차단)
  (tabs)/         홈 · 운동 · 영양 · 기록 · 내정보
  workout/        프로그램 상세, 운동 상세, 루틴 플레이어
  nutrition/      영양 추천 결과

src/
  components/     공용 UI (Button, Card, PainSlider, BodyRegionSelector ...)
  data/           운동·프로그램·영양 콘텐츠 (코드가 아니라 데이터)
  features/
    assessment/   설문 타입·문항·판정 엔진·라우트 가드
    exercise/     운동 규칙·루틴 조립
    nutrition/    영양 규칙·가이드 생성
    logs/         기록 저장
  theme/          색상·글자·간격 토큰
  utils/          저장소 래퍼, 안전 문구, 날짜

docs/             기획 문서 5종
```

### 안전 장치가 들어간 곳

| 파일 | 역할 |
|---|---|
| `src/features/assessment/assessmentEngine.ts` | 위험 신호 판정, 위험도 분류, 통증 컷오프, 운동 중 조정 |
| `src/features/assessment/routeGuard.ts` | 상태별 접근 가능 화면 판정 |
| `src/components/SafetyGate.tsx` | 화면 이동마다 검사 (딥링크 우회 차단) |
| `src/features/nutrition/nutritionRules.ts` | 섭식장애 위험 시 감량 플랜 차단, 학생 모드 숫자 제거 |
| `app/assessment/blocked.tsx` | 상담 안내, 뒤로가기 차단, 대체 루틴 미제공 |

---

## 전문가 검수 (M7)

검수자에게 건넬 문서를 자동으로 만듭니다. 앱을 설치하지 않아도 종이로 검토할 수 있습니다.

```bash
npm run review:packet
```

| 만들어지는 것 | 용도 |
|---|---|
| **`docs/review-packet.pdf`** | **바로 인쇄해서 건네는 용도** |
| **`docs/검수표.xlsx`** | **엑셀로 열어 O·△·X 를 표시하는 용도** |
| `docs/review-packet.html` | 인쇄용 원본 (브라우저에서 열어 PDF로 저장) |
| `docs/REVIEW_PACKET.md` | 저장소용 (버전 관리) |

엑셀 검수표를 다시 만들려면:

```bash
npm run review:workbook
```

엑셀 파일은 **노란색 칸에만** 적으면 되고, '판정' 칸은 O / △ / X 드롭다운입니다.
'진행현황' 시트가 표시한 개수를 자동으로 세어 줍니다.

담긴 내용: 위험 신호 12문항 / 위험도 4단계 / 통증 배정 기준 / 운동 중 대응 기준 /
운동 86개 전체 / 영양 4트랙 / 학생 보호 규칙 / 검수자 서명란

각 항목에 **구체적인 질문**과 O·△·X 표시란이 있어, 검수자가 바로 판단을 적을 수 있습니다.
코드가 바뀌면 다시 실행해 주세요. 문서와 코드가 어긋나면 테스트가 실패합니다.

## ⚠️ 검수 전 항목 (중요)

아래 항목은 **임상 근거로 검증된 값이 아닙니다.** 실제 사용자에게 배포하기 전에
건강운동관리사 · 물리치료사 · 의사 · 영양 전문가의 검수가 필요합니다.

| 항목 | 현재 값 | 출처 |
|---|---|---|
| 위험 신호 12개 항목 | `assessmentTypes.ts` | 기획 단계에서 정한 목록 |
| 운동 제공 중단 기준 | 통증 7점 이상 | 기획 단계에서 정한 값 |
| 1단계 배정 기준 | 통증 4~6점 | 기획 단계에서 정한 값 |
| 2단계 배정 기준 | 통증 1~3점 | 기획 단계에서 정한 값 |
| 운동 중 중단 기준 | 통증 6점 이상 | 기획 단계에서 정한 값 |
| 탄단지 비율 | `mealTemplates.ts` | 기획 단계에서 정한 값 |
| 운동 동작 선택과 단계 배치 | `src/data/exercises/` | 기획 단계에서 정한 구성 |
| 체중당 단백질 g/kg | `mealTemplates.ts` | 2026-09-02 사용자 지정값. ISSN 지침 참고 |
| 부위별 손상 설명 | `injuryEducation.ts` | 일반적으로 알려진 내용. **의료 전문가 검수 전** |
| 유연성·균형 운동 권고 | `activityGuidelines.ts` | ⚠️ 원문 미확인 (`verified: false`) |
| 청소년 활동량 권고 | `activityGuidelines.ts` | ⚠️ 원문 미확인 (`verified: false`) |
| 개인정보 수집·이용 안내문 | `src/utils/privacy.ts` | **법률 검토 전** |

기준값은 `src/features/assessment/assessmentEngine.ts` 상단 상수 한 곳에 모여 있어,
검수 결과가 나오면 그 파일만 고치면 됩니다.

운동 지침(`activityGuidelines.ts`)은 항목마다 `verified` 표시가 있습니다.
`verified: false` 인 항목은 앱 화면에도 **'확인 필요'** 라고 그대로 나옵니다.
확인하지 못한 것을 확인한 것처럼 쓰지 않기 위해서입니다.

동작 영상 목록은 2026-09-02에 **앱 제작자 본인이 자체 점검**한 것이며,
외부 전문가 검수를 받은 것이 아닙니다. ([docs/VIDEO_CANDIDATES.md](docs/VIDEO_CANDIDATES.md))

미확정 항목 전체 목록은 [docs/CONTENT_GUIDE.md §9](docs/CONTENT_GUIDE.md) 참고.
